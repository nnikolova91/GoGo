@using Microsoft.AspNetCore.Identity
@model ViewModels.GameDetailsViewModel
@inject UserManager<GoUser> UserManager
@{
    ViewData["Title"] = "Create";
}

<main class="mt-3">
    
    <div>
        <h4>Title:  @Model.Name</h4>
        <h5>Description:  @Model.Description</h5>
    </div>
    <div >
        <div ><h4><b>Level 1</b></h4></div>
        <div ><h4><b>Points - @Model.Level1.Points</b></h4></div>
        <div>
            @{ var base64 = Convert.ToBase64String(@Model.Level1.Image);
                var imgSrc = String.Format("data:image/gif;base64,{0}", base64);
                <img src="@imgSrc" /> }
        </div>
        <div ><h4><b>@Model.Level1.Description</b></h4></div>
        <div >

            <form method="post" asp-action="AddImage" asp-controller="Games" asp-route-id="@Model.Id" asp-route-levelId="@Model.Level1.Id" enctype="multipart/form-data">
                <div class="form-group">
                    <label asp-for="@Model.Level1.CorrespondingImage"></label>
                    <input asp-for="@Model.Level1.CorrespondingImage" type="file" onchange="readURL(this);" />
                    <span asp-validation-for="@Model.Level1.CorrespondingImage" class="text-danger"></span>
                    <img id="blah" src="#" alt="your image" />
                </div>
                <button type="submit" >AddImage</button>
            </form>
        </div>
        <div >
            @foreach (var item in Model.GameParticipantsLevel1)
            {
                var base66 = Convert.ToBase64String(item.CorrespondingImage);
                    var imgSrccc = String.Format("data:image/gif;base64,{0}", base66);
                    <img src="@imgSrccc" /> 
            }
        </div>
    </div>
    <h6>-----------------------------------------------------------------------------</h6>
    <div style=" padding-top:30px;" >
        <div @*class="inline col-sm-1"*@><h4><b>Level 1</b></h4></div>
        <div @*class="inline col-sm-1"*@><h4><b>Points - @Model.Level2.Points</b></h4></div>
        <div @*class="inline col-sm-2"*@>
            @{ var base65 = Convert.ToBase64String(@Model.Level2.Image);
                var imgSrcc = String.Format("data:image/gif;base64,{0}", base65);
                <img src="@imgSrcc" /> }
        </div>
        <div @*class="inline col-sm-2"*@><h4><b>@Model.Level2.Description</b></h4></div>
        <div @*class="inline col-sm-1"*@>
            <form method="post" asp-action="AddImage" asp-controller="Games" asp-route-id="@Model.Id" asp-route-levelId="@Model.Level2.Id" enctype="multipart/form-data">
                <div class="form-group">
                    <label asp-for="@Model.Level2.CorrespondingImage"></label>
                    <input asp-for="@Model.Level2.CorrespondingImage" type="file" onchange="readURL(this);" />
                    <span asp-validation-for="@Model.Level2.CorrespondingImage" class="text-danger"></span>
                    <img id="blah" src="#" alt="your image" />
                </div>
                <button type="submit">AddImage</button>
            </form>
        </div>
        <div @*class="inline col-sm-2"*@>
            @foreach (var item in Model.GameParticipantsLevel2)
            {
                var base66 = Convert.ToBase64String(item.CorrespondingImage);
                var imgSrccc = String.Format("data:image/gif;base64,{0}", base66);
                <img src="@imgSrccc" />
            }
        </div>
    </div>


    @if (this.User.Identity.IsAuthenticated)
    {
        <form method="post" asp-action="AddLevel" asp-controller="Games" asp-route-id="@Model.Id">
            <div class="inline col-sm-2">
                <button>AddLevel</button>
            </div>
        </form>
    }
        <div >
            @if (this.User.Identity.IsAuthenticated)
            {
                <form method="post" asp-action="Start" asp-controller="Games" asp-route-id="@Model.Id">
                    <div class="text-center" style="padding-bottom:20px; padding-top:200px;">
                        <button class="btn">Start</button>
                    </div>
                </form>
            }
        </div>
    @*@if (this.User.Identity.IsAuthenticated && ViewData["CurrentUser"].ToString() == Model.Creator.Id)
        {
        <form asp-action="AddResults" asp-controller="Cources" asp-route-id="@Model.Id">
            <div class="text-center">
                <button class="btn chushka-bg-color text-center">Add results</button>
                <h6>------------------------------------------------------------------------------------------</h6>
            </div>

        </form>
        }*@
    @*@if (this.User.Identity.IsAuthenticated)
    {
        <form asp-action="SignIn" asp-controller="Cources" asp-route-id="@Model.Id">
            <div class="text-center">
                <button class="btn chushka-bg-color text-center">Have Part</button>
                <h6>------------------------------------------------------------------------------------------</h6>
            </div>
        </form>
    }
    else
    {
        <div class="text-center">
            <div class="alert alert-primary" role="alert">
                <button class="btn chushka-bg-color text-center" onclick="myFunction()">Sign in</button>
            </div>
        </div>
    }
    <hr />
    <form asp-action="All" asp-controller="Cources">
        <div class="text-center">
            <button asp-action="All" asp-controller="Cources" class="btn chushka-bg-color text-center">Back To All</button>
            <h6>-------------------------------------------------------------------------------------------</h6>
        </div>

    </form>*@

</main>