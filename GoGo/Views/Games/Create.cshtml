@model ViewModels.CreateGameViewModel
@using ViewModels
@{
//ViewData["Title"] = "Create";
}

<main class="mt-3">
    <h1 class="text-center">Create Game</h1>
    <hr class="wiy chushka-bg-color height: 7px" />
    <form class="mx-auto half-width wiy" method="post" asp-controller="Games" asp-action="Create" enctype="multipart/form-data">
        <div class="row">

            <div class="form-group inline col-sm-6  text-center">
                <label asp-for="Name"><b>Title</b></label>
                <input asp-for="Name" type="text" class="form-control" placeholder="Name...">
            </div>

            <div class="form-group inline col-sm-6  text-center">
                <label asp-for="LevelsCount"><b>Levels count</b></label>
                <input id="form1"  asp-for="LevelsCount" type="number" class="form-control" placeholder="LevelsCount...">
            </div>

        </div>

        @*@foreach(var item in Model.Levels)
        {
            <div class="form-group inline col-sm-6  ">
                <label asp-for="@item.Description"><b>Description</b></label>
                <input asp-for="@item.Description" type="text" class="form-control" placeholder="Description...">
            </div>
            <div class="form-group inline col-sm-1  text-center">
                <label asp-for="@item.Points"><b>points</b></label>
                <input asp-for="@item.Points" type="number" class="form-control" placeholder="Points...">
            </div>
            <div class="form-group inline col-sm-1">
                <label asp-for="@item.Image"></label>
                <input asp-for="@item.Image" type="file" onchange="readURL(this);" />
                <img id="blah" src="#" alt="your image" />
            </div>
        }}*@
        
        <div class="row">
            <div class="form-group  inline col-sm-12  text-center">
                <label asp-for="Description"><b>Description and recommendations</b></label>
                <input asp-for="Description" type="text" class="form-control" placeholder="Description...">
            </div>
        </div>
        

        <hr class="chushka-bg-color height: 7px" />
        <div class="text-center">
            <button type="submit">AddLevels</button>
        </div>
    </form>
    
        <script>
            var numb = $("#CountOfLevels").val();
            
            }


            @*function GetCountOfLevels() {
                var LevelsCount = document.forms.form1.LevelsCount
                @ViewBag["a"] = document.getElementById("form1").innerHTML;
                
                    //alert(num);
            }*@
        </script>

    @*@{
            if (Model.LevelsCount > 0)
            {
                for (int i = 0; i < Model.LevelsCount; i++)
                {
                    Model.Levels.Add(new LevelViewModel());
                }
            }
        }*@
    @*@if (Model.Levels.Count() > 0)
        {
            int count = 1;

            foreach (var item in Model.Levels)
            {
                <h5>Level @count</h5>
                <div class="form-group inline col-sm-6  text-center">
                    <label asp-for="@item.Description"><b>Description</b></label>
                    <input asp-for="@item.Description" type="text" class="form-control" placeholder="Description...">
                </div>
                <div class="form-group inline col-sm-6  text-center">
                    <label asp-for="@item.Points"><b>points</b></label>
                    <input asp-for="@item.Points" type="number" class="form-control" placeholder="Points...">
                </div>
            }
        }*@


</main>
